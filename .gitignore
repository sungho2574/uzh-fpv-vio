.DS_Store

# Extracted ASCII/image copy of the dataset -- redundant with the .bag
# (already tracked via git-lfs) and far too large (events.txt alone is ~1GB).
data/indoor_forward_9_davis_with_gt/


# Pipeline outputs (regenerated by running the pipeline)
output/
results/
